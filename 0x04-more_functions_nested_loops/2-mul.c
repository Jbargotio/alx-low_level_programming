#include "main.h"
/**
 * mul - multiplies two integers
 *@a: number being tested
 *@b: number being tested
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
